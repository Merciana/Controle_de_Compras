# Nome da aplicação
spring.application.name=controle_de_compras

# Configuração do banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/database_planilha?useSSL=false&serverTimezone=UTC
spring.datasource.username=Merciana
spring.datasource.password=Mercy031!@#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuração do JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update  # Pode ser 'create', 'create-drop', 'validate', 'none'
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# HikariCP (pool de conexões)
spring.datasource.hikari.maximum-pool-size=10

# Log de SQL (opcional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
